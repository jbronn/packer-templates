{
    "post-processors": [
        {
            "type": "amazon-import",
            "access_key": "{{user `access_key`}}",
            "secret_key": "{{user `secret_key`}}",
            "region": "{{user `region`}}",
            "s3_bucket_name": "{{user `s3_bucket_name`}}",
            "license_type": "BYOL",
            "tags": {
                "Description": "CentOS 7 Minimal {{timestamp}}"
            }
        }
    ],
    "variables": {
        "access_key": null,
        "cloud_init": "yes",
        "disk_type": "plain",
        "format": "ova",
        "guest_additions_mode": "disable",
        "region": null,
        "s3_bucket_name": null,
        "secret_key": null
    }
}
